import 'package:flutter/material.dart';

class DonorDashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Donor Dashboard")),
      body: Center(child: Text("Donor Home Screen")),
    );
  }
}
